{
  "name": "solar-swing",
  "version": "0.7.0",
  "private": true,
  "type": "module",
  "description": "3D orbital traversal sandbox — jetpack across a relative-scale solar system",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "three": "0.164.1"
  },
  "devDependencies": {
    "@types/three": "0.164.1",
    "typescript": "^5.4.5",
    "vite": "^5.2.0"
  }
}
